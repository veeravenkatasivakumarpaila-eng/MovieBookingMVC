  MovieBookingMVC
│
├── Controllers/
│   └── Handles HTTP requests and responses
│
├── Models/
│   └── database entities
│
├── Views/
│   └── UI pages ( JSP )
│
├──  public/
│   └── Static files (CSS, JS, images)
│
└── README.md
